services:
  - type: web
    name: insurance-premium-prediction-mlops
    runtime: python
    buildCommand: pip install -r requirements_api.txt
    startCommand: uvicorn serving.insurance_premium_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
